HTML and scripts for reporting daily processing.  

Assumes two subdirectories:

* a results directory generated by the analyse_cors python script
* a daily_processing directory containing files yyyy/ddd/R2Syyddd0.PRC

Also assumes (prefers) that .PRC files are returned as text/plain content type
